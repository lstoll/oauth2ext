// Package jwt contains shared JOSE helpers used by the public jwt package
// and protocol packages. It is not a token-verification API: callers must
// not treat its outputs as trusted without applying the appropriate
// protocol checks.
package jwt
